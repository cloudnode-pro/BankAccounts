name: BankAccounts
version: '${project.version}'
main: pro.cloudnode.smp.bankaccounts.BankAccounts
api-version: "1.20"
author: Cloudnode
softdepend:
    - PlaceholderAPI
    - Vault
libraries:
    - org.mariadb.jdbc:mariadb-java-client:3.4.0
    - com.zaxxer:HikariCP:5.1.0
commands:
    bank:
        description: Bank Accounts
        usage: /<command> help
    pos:
        description: Create point of sale
        usage: /<command>
    baltop:
        description: Balance leaderboard
        usage: /<command>
    invoice:
        description: Invoicing and payment requests
        usage: /<command>
